# Copiá este archivo a .env y ajustá según tu entorno.
# Coincide con el docker-compose.yml (Postgres local en la LAN de la sucursal).
DATABASE_URL="postgresql://pos:pos@localhost:5432/pos?schema=public"

# Identifica el nodo/sucursal (se usa desde la Etapa 1/Slice B en adelante).
# SUCURSAL_ID="..."
